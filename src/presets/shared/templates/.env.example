# MCP / API keys (copy to .env — never commit .env)
# Optional — Context7 docs MCP works without a key for many queries
CONTEXT7_API_KEY=

# Optional — uncomment as needed
# FIGMA_API_KEY=
# DATABASE_URL=
